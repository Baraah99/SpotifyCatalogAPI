scrape_configs:
  - job_name: 'spotify_frontend'
    scrape_interval: 5s           # Scrapes every 15 seconds
    scrape_timeout: 5s            # Timeout for each scrape request
    metrics_path: '/actuator/prometheus'
    scheme: 'https'
    static_configs:
      - targets: ['a808-212-199-36-114.ngrok-free.app']
